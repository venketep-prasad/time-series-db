ROOT
  PIPELINE
    FUNCTION(fetch)
      TAG_KEY(service)
        VALUE("web-api")
      TAG_KEY(region)
        ARGS(["us-east", "us-west"])
      TAG_KEY(name)
        VALUE("requests_total")
    FUNCTION(perSecond)
    FUNCTION(aliasByTags)
      VALUE(service)
      VALUE(region)
    FUNCTION(sort)
      VALUE(avg)
