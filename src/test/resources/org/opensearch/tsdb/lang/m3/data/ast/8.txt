ROOT
  PIPELINE
    PIPELINE
      GROUP
        FUNCTION(fetch)
          TAG_KEY(name)
            VALUE(queries)
        FUNCTION(sum)
    PIPELINE
      GROUP
        FUNCTION(fetch)
          TAG_KEY(name)
            VALUE(exceptions)
        FUNCTION(sum)
